import 'package:flutter/material.dart';

class RepositoryTab extends StatelessWidget {
  const RepositoryTab({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child:  Text('Repository Tab'),
    );
  }
}